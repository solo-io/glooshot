apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: stats-app-1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: stats-app-1
  template:
    metadata:
      labels:
        app: stats-app-1
    spec:
      containers:
        - name: stats-app
          image: soloio/stats-app:v0.0.2
          imagePullPolicy: Always
          env:
            - name: NEIGHBOR_INDEX
              value: "1"
            - name: NEIGHBOR_SERVICE_LIST
              value: stats-app-0,stats-app-1,stats-app-2
          ports:
            - containerPort: 8080
              protocol: TCP
---
kind: Service
apiVersion: v1
metadata:
  name: stats-app-1
spec:
  selector:
    app: stats-app-1
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
